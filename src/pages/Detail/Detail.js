import React from 'react';
import CoinHistory from '../../components/CoinHistory/CoinHistory';

const Detail = () => {
  return (
    <div data-testid='detail-test'>
      <CoinHistory />
    </div>
  );
};

export default Detail;
